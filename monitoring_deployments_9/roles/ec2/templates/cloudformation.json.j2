{
    "Resources": {
        "NagiosServer": {
            "Type" : "AWS::EC2::Instance",
            "Properties" : {
                "AvailabilityZone" : "{{ availability_zone }}",
                "ImageId" : "{{ ubuntu_ec2_image }}",
                "InstanceType" : "{{ instance_type }}"
            }
        },
        "NagiosHost": {
            "Type" : "AWS::EC2::Instance",
            "Properties" : {
              "AvailabilityZone" : "{{ availability_zone }}",
              "ImageId" : "{{ mean_ec2_image }}",
              "InstanceType" : "{{ instance_type }}"
            }
        }
    }
}
